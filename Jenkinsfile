pipeline {
	agent any
	stages {	
		stage("build"){
			steps {
				echo "building..."
				echo "testing auto trigger..."
				echo "does this work??????????????????????"
				echo "does not work..."
			}
			
		}
		stage("test") {
			steps {
				echo "testing..."
			}
		}
		stage("deploy") {
			steps {
				echo "deploying..."
			}
		}
	}
}
